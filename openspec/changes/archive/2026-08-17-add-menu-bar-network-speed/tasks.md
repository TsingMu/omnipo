# Tasks：add-menu-bar-network-speed

## 1. OpenSpec 文档

- [x] 1.1 编写 `proposal.md`，明确双行网速展示、范围、非目标与成功标准。
- [x] 1.2 编写 `design.md`，定义 AppKit 展示边界、数据流、采样生命周期、格式化与降级方案。
- [x] 1.3 编写 `tasks.md`，拆分模型、采样、状态栏 UI、设置响应、测试与验收任务。
- [x] 1.4 编写 `specs/application-foundation/spec.md` 增量规范。

## 2. 速率展示模型与格式化

- [x] 2.1 定义菜单栏整机上传/下载速率快照和 warming-up / available / unavailable 状态。
- [x] 2.2 实现紧凑速率格式化器，覆盖 `B/s`、`K/s`、`M/s`、`G/s`。
- [x] 2.3 处理负值、`NaN`、无穷值和单位边界，确保输出长度受控。
- [x] 2.4 为格式化器和上传/下载字段映射编写单元测试。

## 3. 菜单栏网络采样生命周期

- [x] 3.1 实现轻量 `MenuBarNetworkRateMonitor` actor，复用 `NetworkSampler` 而不启动完整 `SystemMonitorService`。
- [x] 3.2 实现 1 秒采样循环，使用实际采样时间差计算速率。
- [x] 3.3 首次采样发布 warming-up；第二次起发布 available；失败发布 unavailable。
- [x] 3.4 实现幂等 start、可取消 stop、结束更新流和 previous 清理。
- [x] 3.5 隔离采样 generation，确保 stop 或重启后的晚到结果不更新新会话。
- [x] 3.6 为首次预热、第二次速率、失败降级、重复 start、stop 和 restart 编写测试。

## 4. 状态栏双行视图

- [x] 4.1 新增轻量 AppKit 展示视图：左侧 Omnipo 模板图标，右侧上传在上、下载在下。
- [x] 4.2 使用系统小号字体、等宽数字、语义文字色和稳定宽度布局。
- [x] 4.3 让展示视图不参与 hit testing，保留 `NSStatusBarButton` 对整个区域的菜单点击行为。
- [x] 4.4 将状态项从方形长度调整为可容纳图标和双行文本的稳定长度，并适配按钮 bounds 变化。
- [x] 4.5 添加 accessibility label/value，明确区分上传和下载；禁止每秒主动播报。
- [x] 4.6 保留图标资源与系统符号后备路径，布局失败时优先保留菜单入口。

## 5. StatusBarManager 集成

- [x] 5.1 在 `StatusBarManager` 中持有唯一 monitor、订阅任务和展示视图，保持 `setup()` 幂等。
- [x] 5.2 状态项显示时启动采样并显示 `↑ --` / `↓ --`；收到更新后在 MainActor 刷新 UI。
- [x] 5.3 状态项隐藏时先停止采样、取消订阅并清空旧结果，再设置不可见。
- [x] 5.4 再次显示时重新预热，不使用隐藏期间的旧 previous。
- [x] 5.5 保留现有菜单构建、打开主窗口、Launcher、导航、设置和退出行为。
- [x] 5.6 manager 释放或应用退出时取消所有持有任务，避免引用环和后台残留。

## 6. 设置与日志

- [x] 6.1 沿用 `showMenuBarIcon` 和 `.menuBarVisibilitySettingDidChange`，不新增独立网速开关。
- [x] 6.2 验证“恢复 Clippy 风格默认设置”同样触发正确的采样启停。
- [x] 6.3 审计日志，确认不记录具体上传/下载速率、接口名或时间戳。
- [x] 6.4 确认实现不申请额外权限、不持久化速率、不产生网络请求。

## 7. 构建、测试与验收

- [x] 7.1 运行相关单元测试并修复失败。
- [x] 7.2 运行 Debug 构建，确保 Swift 6 严格并发检查通过且无新增警告。
- [x] 7.3 人工验证上传在上、下载在下，速率方向不颠倒。
- [x] 7.4 人工验证图标和两行文字任意位置均可点击打开原有菜单。
- [x] 7.5 人工验证浅色/深色、显示缩放和刘海屏下不裁切且可读。
- [x] 7.6 人工验证设置隐藏后采样停止，再次显示时先预热后恢复。
- [x] 7.7 制造上传、下载和空闲三类流量，与活动监视器做数量级对比。
- [x] 7.8 完成 VoiceOver 检查，确保内容可读且不会每秒主动打断用户。
- [x] 7.9 OpenSpec 严格校验通过后更新任务状态；实现验收后合并规格并归档 change。
