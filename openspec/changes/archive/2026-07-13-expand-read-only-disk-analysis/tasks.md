# Tasks: Expand Read-Only Disk Analysis

> 本 change 只扩展只读磁盘分析，不删除、移动、重命名或修改任何用户文件。每完成一项必须更新 `[ ]` 为 `[x]`，并保持工程可编译。

## 1. 规范与基线审计

- [x] 1.1 评审 proposal、design 与 disk-analysis delta spec，确认只读边界和非目标。
- [x] 1.2 记录 `v0.1.0` 构建、全量测试与 OpenSpec strict 基线，不提交设备名、真实路径或文件清单。
- [x] 1.3 审计 Cleaner 当前文案、刷新入口、列表状态和 Finder action 可复用边界。
- [x] 1.4 确认不新增 Cleaner 删除服务、废纸篓执行、持久文件索引、遥测、云服务或发布流程。

## 2. 元数据分类与查询模型

- [x] 2.1 定义文件类型、大小、修改时间和目录 facet 值模型，未知值使用保守 fallback。
- [x] 2.2 实现纯函数分类器，只使用名称扩展名、大小、修改时间和授权根相对路径。
- [x] 2.3 定义查询文本、facet 组合和排序模型，确保组合过滤结果可预测。
- [x] 2.4 实现当前结果的可见数量、可见字节、已选数量和已选字节聚合。
- [x] 2.5 覆盖类型、边界大小、时间注入、未知修改时间、目录 fallback、组合过滤和稳定排序测试。

## 3. Workbench 状态与刷新边界

- [x] 3.1 实现 MainActor `LargeFileWorkbenchStore`，统一管理查询、筛选、排序、选择、忽略和 reveal 提示。
- [x] 3.2 实现选择、批量选择、忽略、恢复和 review 状态不变量；忽略项不得继续保持选中。
- [x] 3.3 新扫描结果替换时清空会话内选择/忽略状态，不持久化路径派生身份。
- [x] 3.4 将容量刷新与目录扫描刷新拆分为独立动作；筛选和排序不得触发服务调用。
- [x] 3.5 覆盖结果替换、无匹配、全部忽略、选择汇总和刷新调用边界测试。

## 4. 只读 Workbench UI

- [x] 4.1 修正 Cleaner 标题、说明和 Phase 0 文案，准确描述已实现与仍未实现能力。
- [x] 4.2 增加当前授权/capped result 边界说明和四项聚合摘要。
- [x] 4.3 增加搜索、类型、大小、修改时间、目录和排序控件，并提供清除筛选操作。
- [x] 4.4 扩展文件行，展示选择状态、元数据类型、大小、修改时间和路径。
- [x] 4.5 增加已选/已忽略 review、恢复操作及无结果、无匹配、全部忽略的不同空状态。
- [x] 4.6 为筛选控件、结果行、选择、忽略和 review 补充键盘与辅助功能语义。

## 5. Finder 定位与授权释放

- [x] 5.1 定义可注入的 `LargeFileRevealService` 与稳定、脱敏的结果模型。
- [x] 5.2 实现当前结果校验、授权根范围校验、文件存在性检查和 Finder 定位。
- [x] 5.3 确保 success、missing、out-of-root、scope denied 和 unexpected failure 均释放 security scope。
- [x] 5.4 UI 只为当前可用记录提供 Finder 操作，失败时展示不含路径和文件名的恢复提示。
- [x] 5.5 覆盖 Finder 调用、拒绝路径、失效文件、scope 释放和日志隐私测试。

## 6. 验证与本地验收

- [x] 6.1 运行分类、store、UI mapping、AppState 和 reveal focused tests。
- [x] 6.2 运行 `openspec validate --all --strict` 和全量 `./script/build_and_run.sh verify`。
- [x] 6.3 人工验证授权扫描、容量单独刷新、扫描单独刷新、组合筛选、选择/忽略/恢复和 Finder 定位。
- [x] 6.4 验证页面无删除、废纸篓、移动、重命名或自动清理入口，文件系统内容无变化。
- [x] 6.5 验收证据只记录通过/失败和稳定原因码，不提交真实路径、文件名、目录名或截图。

## 7. 收尾

- [x] 7.1 审计新增日志、错误和设置，确认不包含路径、文件名、查询文本、目录 facet 或 bookmark 数据。
- [x] 7.2 更新 `openspec/project.md` 的磁盘分析基线与仍未交付范围。
- [x] 7.3 审阅任务状态，确认无人工验收项被提前标记。
- [x] 7.4 合并 delta spec、归档 change，并在通过全部门禁后创建下一本地稳定标签。
