# main-dashboard 增量规范

## ADDED Requirements

### Requirement: 侧栏必须提供清晰且不重复的焦点反馈

系统 MUST 保留侧栏键盘导航能力，并以入口选中高亮作为主要状态反馈。侧栏滚动容器获得键盘焦点时不得额外显示包围整个侧栏的系统焦点描边。

#### Scenario: 侧栏获得键盘焦点

- **当** 用户通过键盘与侧栏交互
- **那么** 整个侧栏外围不显示蓝色焦点框
- **并且** 当前入口仍显示明确的选中高亮
- **并且** 上下方向键仍按稳定顺序切换入口
