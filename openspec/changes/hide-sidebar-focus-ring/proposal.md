# Proposal：隐藏侧栏外层焦点描边

## 背景

稳定侧栏为支持方向键导航而让滚动容器可聚焦。macOS 会为获得焦点的容器自动绘制蓝色焦点框，视觉上像侧栏多出一圈边框，与当前主窗口设计不一致。

## 目标

- 隐藏侧栏滚动容器的系统焦点描边。
- 保留方向键导航、选择状态、滚动能力和辅助功能语义。
- 不改变其他页面布局和窗口焦点行为。

## 非目标

- 不移除侧栏的键盘导航能力。
- 不修改导航入口样式或详情页布局。
